numpy==1.23 ; python_version >= "3.8"  # From recipe.yml, placed first for biopython
biopython==1.69 ; python_version >= "3.8"  # From previous requirements.txt
fastapi==0.115.12 ; python_version >= "3.8"  # From setup.py, pinned for stability
uvicorn==0.34.2 ; python_version >= "3.8"  # From previous requirements.txt, Python 3.10+
python-multipart==0.0.20 ; python_version >= "3.8"  # From setup.py
pandas==2.2.3 ; python_version >= "3.8"  # From setup.py, Python 3.10+ version
pydantic==2.11.4 ; python_version >= "3.8"  # From setup.py, Python 3.10+ version
requests==2.32.3 ; python_version >= "3.8"  # From setup.py
perf_ssr==0.4.8 ; python_version >= "3.8"  # From setup.py, recipe.yml
plotly==6.0.1 ; python_version >= "3.8"  # From setup.py, pinned from previous requirements.txt
kaleido==0.2.1.post1 ; python_version >= "3.8"  # For plotly, fixed version
plotly-upset-hd==0.0.2 ; python_version >= "3.8"  # From setup.py
typer==0.15.3 ; python_version >= "3.8"  # From recipe.yml, simplified from typer[all]
rich==14.0.0 ; python_version >= "3.8"  # From recipe.yml, for typer and rich-click
rich-click==1.8.8 ; python_version >= "3.8"  # From setup.py
argcomplete==3.6.2 ; python_version >= "3.8"  # From setup.py, pinned from previous requirements.txt